import { StationFilterPipe } from './station-filter.pipe';

describe('StationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
